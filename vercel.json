{
  "buildCommand": "pip install -r requirements.txt && python3 scrape.py && python3 build.py",
  "outputDirectory": ".",
  "framework": null,
  "rewrites": [
    { "source": "/", "destination": "/rooms.html" }
  ]
}
